import * as React from 'react';
import { cn } from '../lib/utils'; export interface FormInputProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'size'> { label: string; error?: string; helperText?: string; required?: boolean; variant?: 'default' | 'filled' | 'outlined'; size?: 'sm' | 'md' | 'lg'; fullWidth?: boolean;
} const FormInput = React.forwardRef<HTMLInputElement, FormInputProps>( ( { className, label, error, helperText, required, variant = 'default', size = 'md', fullWidth = true, id, ...props }, ref ) => { const inputId = id || `form-input-${Math.random().toString(36).substring(2, 11)}`; const variantClasses = { default: 'bg-white border border-near-black focus:border-apple-blue focus:ring-2 focus:ring-apple-blue/20', filled: 'bg-near-black border border-transparent focus:bg-white focus:border-apple-blue focus:ring-2 focus:ring-apple-blue/20', outlined: 'bg-transparent border-2 border-near-black focus:border-apple-blue focus:ring-2 focus:ring-apple-blue/20', }; const sizeClasses = { sm: 'h-10 px-3 text-sm rounded-lg', md: 'h-12 px-4 text-base rounded-xl', lg: 'h-14 px-5 text-lg rounded-lg', }; return ( <div className={cn('space-y-2', fullWidth && 'w-full')}> {label && ( <label htmlFor={inputId} className="block text-sm font-semibold text-near-black" > {label} {required && <span className="text-near-black ml-1">*</span>} </label> )} <input id={inputId} ref={ref} className={cn( 'w-full transition-all duration-200 outline-none placeholder:text-near-black', variantClasses[variant], sizeClasses[size], error && 'border-near-black focus:border-near-black focus:ring-near-black/20', className )} aria-invalid={!!error} aria-describedby={error ? `${inputId}-error` : helperText ? `${inputId}-helper` : undefined} {...props} /> {error && ( <p id={`${inputId}-error`} className="text-sm text-near-black"> {error} </p> )} {helperText && !error && ( <p id={`${inputId}-helper`} className="text-sm text-near-black"> {helperText} </p> )} </div> ); }
); FormInput.displayName = 'FormInput'; export { FormInput };