{
  "name": "@tripalfa/static-data",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./frontend": "./src/frontend.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "tsc --watch",
    "test": "vitest run --passWithNoTests",
    "sync:liteapi": "echo \"LiteAPI sync moved to infrastructure pipelines\"",
    "sync:exchange-rates": "echo \"FX sync moved to infrastructure pipelines\"",
    "sync:timezone": "echo \"Timezone sync moved to infrastructure pipelines\"",
    "sync:start": "echo \"Static data sync scheduler is externalized\""
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^4.0.18"
  }
}
