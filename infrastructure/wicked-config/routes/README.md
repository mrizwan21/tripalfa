# Wicked Route Catalog

`platform-core-routes.json` is the canonical route mapping used to generate Kong declarative config.

Do not add parallel legacy Kong route files here. Extend the single route catalog instead.
