# TripAlfa API Catalog

This catalog is the Wicked-style source of truth for API surfaces managed by Kong.

It includes:

- Shared internal platform services
- Frontend app specific API surfaces
- External supplier integrations (proxied through Kong)

All frontend apps are expected to consume APIs through the gateway endpoint only.
