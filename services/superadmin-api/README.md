# Super Admin API (minimal scaffold)

This is a minimal scaffold for the Super‑Admin API used to aggregate tenants, notifications and analytics for the platform. It's intentionally small and designed to be extended.

Run locally:

```bash
cd services/superadmin-api
npm ci
npm run dev
```
